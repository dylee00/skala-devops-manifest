apiVersion: v1
kind: Service
metadata:
  name: sk050-k8s-rag
  namespace: sk050-app
spec:
  selector:
    app: sk050-k8s-rag
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: http
  type: ClusterIP
